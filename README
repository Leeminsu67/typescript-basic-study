# TypeScript 설치 및 버전 확인 ✅

간단한 설치 및 버전 확인 기록입니다.

## 1) Node / npm 버전 확인
```
$ node --version
v24.11.1

$ npm --version
11.6.2
```

## 2) TypeScript 전역 설치
```
$ npm install typescript --global
added 1 package in 839ms
```

## 3) tsc (TypeScript Compiler) 버전 확인
```
$ tsc --version
Version 5.9.3
```

## 4) typescript 프로젝트내 초기화(설치)
```
$ tsc --init
```

## 학습 자료 요약 🔎
- 1_basic
- `1_problem_with_js.js`  
  - JavaScript의 동적 타입으로 발생할 수 있는 문제를 보여줍니다. `add()`에 숫자가 아닌 문자열을 넘기면 의도치 않은 연결(concatenation) 동작이 발생합니다. `addTypeSafe()` 예시는 런타임에서 typeof로 검사해 오류를 던지는 방법을 설명합니다.
- `1_problem_with_js.ts`  
  - 같은 `add()` 함수를 TypeScript로 작성한 파일입니다. 함수 매개변수에 `number` 타입을 명시해 컴파일 단계에서 잘못된 타입 사용을 방지합니다(예: `add(1, "2")` 호출은 주석 처리되어 있습니다).
- `2_basics.ts`  
  - TypeScript의 기본 타입(문자열, 숫자, bigint 등), `any`/`unknown`/`never`의 설명 및 배열/튜플 예시를 다룹니다.
- `3_type_and_interface.ts`  
  - `type`과 `interface`의 사용법, 유니언 타입과 선택적 속성 등 객체 타입 정의 예시를 설명합니다.
- `4_enum.ts`  
  - `enum`의 기본 동작(숫자값 자동 할당), 문자열 enum 예시 및 상태 관리에서의 사용 방법을 보여줍니다.
- `5_type_inference.ts`  
  - 타입 추론과 `const`를 이용한 리터럴 타입, 배열 및 튜플 추론 예시를 다룹니다.
- `6_casting.ts`  
  - 타입 단언(`as`)의 사용과 런타임에서의 차이, `any`와의 상호작용 예시를 설명합니다.

- 2_union_and_intersection
- `1_union_basics.ts`  
  - 유니언 타입의 기본 개념, 배열·인터페이스와 결합하여 사용하는 방법을 예시로 보여줍니다.
- `2_intersection_basics.ts`  
  - 인터섹션 타입(`&`)으로 여러 타입을 결합하는 방법과 예시를 설명합니다.
- `3_narrowing.ts`  
  - 다양한 내로잉 기법(`typeof`, `in`, `instanceof`, discriminated union 등)과 사용 예시를 정리합니다.

- 3_function
- `1_defining_function.ts`  
  - 함수 정의, 선택/기본/나머지 매개변수, 반환 타입 및 `void`/`never` 등의 특수 반환 타입을 설명합니다.
- `2_function_type.ts`  
  - 함수 타입 별칭과 인터페이스로 함수 타입을 선언하는 방법, 콜백 예시를 보여줍니다.
- `3_overloading.ts`  
  - 함수 오버로딩 선언과 구현 방식, 주의사항을 예시와 함께 설명합니다.
- `4_statement_and_expression.ts`  
  - Statement와 Expression의 차이, 표현식 기반 함수 선언의 장점을 설명하는 계산 함수 예시가 있습니다.
- `5_type_predicate.ts`  
  - 타입 프레디케이트(`input is Type`)의 사용법과 내로잉 정확도 향상 예시를 다룹니다.

- 4_type_and_interface
- `1_type_vs_interface.ts`  
  - `type`과 `interface`의 차이, interface merging 예시 및 각 상황별 권장 사용법을 설명합니다.
- `2_extension.ts`  
  - `extends`와 `&`를 이용한 확장 방법, 다중 확장 및 오버라이딩 관련 차이를 예시로 보여줍니다.

- 5_any_unknown_never
- `1_loop_holes_of_any.ts`  
  - `any` 타입의 허점과 타입 안전성 저하로 인한 문제점, 위험을 설명합니다.
- `2_unknown_type.ts`  
  - `unknown` 타입의 특성(안전성), 타입 가드로의 활용 및 any와의 차이를 다룹니다.
- `3_never_type.ts`  
  - `never` 타입의 사용 사례(예외 던지기, 무한루프, 불가능한 인터섹션) 예시를 정리합니다.

- 6_array
- `1_problems_with_array_in_js.ts`  
  - JavaScript 배열의 문제점과 TypeScript로 타입을 명시할 때의 개선점, 유니언/튜플의 차이를 설명합니다.
- `2_spread_operator.ts`  
  - 스프레드 연산자를 이용한 배열 병합과 타입 추론 예시를 보여줍니다.
- `3_multi_dimension_array.ts`  
  - 다차원 배열의 타입 표기(number[][] 등)와 순회 예시를 다룹니다.

- 7_tuple
- `1_what_is_tuple.ts`  
  - 튜플의 개념, 읽기 전용 튜플과 `as const`를 이용한 튜플 유추, named tuple 예시를 설명합니다.

- 8_object
- `1_defining_object.ts`  
  - 객체 타입 정의(인터페이스·타입 별칭) 및 기본 사용 예시를 보여줍니다.
- `2_property_check.ts`  
  - 초과 속성 검사(Excess Property Checks)와 관련된 할당 동작을 설명합니다.
- `3_nested_object.ts`  
  - 중첩 객체 타입을 분리하여 선언하는 방법과 그 이점(에러 가독성 등)을 다룹니다.
- `4_optional_and_undefined_property.ts`  
  - 선택적(optional) 속성과 `undefined`를 허용하는 방식의 차이와 사용 예시를 설명합니다.
- `5_object_union.ts`  
  - 객체 유니언의 타입 유추 방식과 `in` 연산자를 이용한 내로잉 예시를 다룹니다.
- `6_object_intersection.ts`  
  - 객체 인터섹션 타입으로 여러 타입을 조합하는 방법과 복합 타입의 예시를 보여줍니다.
- `7_key_value_mapping.ts`  
  - Mapped types, `keyof`, `Pick`/`Omit`, 인덱스 접근 타입 등 키-값 매핑 관련 개념을 정리합니다.

- 9_class
- `1_defining_class.ts`  
  - 클래스 선언, 생성자, 인스턴스 프로퍼티 타입 지정 및 메서드 예시를 제공합니다.
- `2_readonly_property.ts`  
  - `readonly` 프로퍼티의 사용과 변경 방지 예시를 보여줍니다.
- `3_property_initialization.ts`  
  - 클래스 프로퍼티 초기화 방법, 선택적 프로퍼티 및 `!`(definite assignment) 사용 예시를 다룹니다.
- `4_class_as_type_and_value.ts`  
  - 클래스가 타입과 값으로 동작하는 방식(구조적 타이핑)과 관련 예시를 설명합니다.
- `5_interface.ts`  
  - 클래스의 인터페이스 구현, 다중 인터페이스 구현과 생성자 타입으로서의 인터페이스 예시를 제공합니다.
- `6_ingeritance.ts`  
  - 상속(`extends`), `super` 사용, 하위/상위 타입 할당 규칙과 optional 프로퍼티 주의사항을 정리합니다.
- `7_override.ts`  
  - 메서드·속성 오버라이드 시 지켜야 할 규칙(파라미터·반환 타입 일치 등)과 예시를 설명합니다.
- `8_abstract_class.ts`  
  - `abstract` 클래스 및 추상 메서드 사용법, 상속 시 구현 강제 예시를 정리합니다.
- `9_visibility_ketword.ts`  
  - `public`/`protected`/`private` 및 JS private(`#`) 키워드의 동작 차이와 접근 범위를 설명합니다.

- 10_generic
- `1_generic_in_function.ts`  
  - 제네릭 함수 사용법(타입 매개변수), 다중 제네릭 및 생성자 인스턴스화 예시를 다룹니다.
- `2_generic_in_interface.ts`  
  - 인터페이스에서 제네릭 사용과 기본 제네릭 타입 설정 예시를 설명합니다.
- `3_generic_in_type.ts`  
  - 타입에서 제네릭을 사용하는 방법, 유니언 상태 타입과 기본 제네릭 적용 예시를 정리합니다.

실습 팁: `.ts` 파일을 빠르게 실행하려면 `tsc`로 컴파일 후 실행하거나, `ts-node`를 설치해 바로 실행할 수 있습니다: `npm i -g ts-node` 또는 프로젝트 레벨에 `npm i -D ts-node`.